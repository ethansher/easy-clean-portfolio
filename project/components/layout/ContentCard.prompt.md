White rounded content block that floats on the blue field — holds body copy, lists, image slots.

```jsx
<ContentCard heading="Research">Survey of 40 P-plate drivers showed…</ContentCard>
```

`heading` is optional (uppercase blue). Default padding `--space-4`; override with `pad`.
