Small painted capsule label, e.g. the cover's "STUDENT NUMBER" tag.

```jsx
<Pill variant="cyan">Student Number</Pill>
<Pill variant="orange">Final Design</Pill>
```

Variants: `cyan` (default), `blue`, `orange`, `white`. Uppercase, tracked-out, display font.
