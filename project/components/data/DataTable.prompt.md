Research/analysis table with electric-blue header band — for pages like "Areas of Investigation".

```jsx
<DataTable
  columns={["Area", "Findings", "Source", "Impact"]}
  rows={[["Time", "78% wash < monthly", "Survey", "High"]]}
/>
```

First cell of each row is emphasised. `tableLayout: fixed` — columns share width evenly.
