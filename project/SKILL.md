---
name: easyclean-design
description: Use this skill to generate well-branded interfaces and assets for Easy Clean — the DNT major-work portfolio design system (compact car-cleaning kit for teenage P-plate drivers). Contains the painted-page brush motif, blue/orange colour system, Archivo/Mulish type, logo, reusable components and a portfolio UI kit, for portfolio pages, slides, mocks and prototypes.
user-invocable: true
---

Read the `readme.md` file in this skill, then explore the other files (tokens, components, `ui_kits/portfolio/`, and the `guidelines/` specimen cards).

If creating visual artifacts (portfolio pages, slides, mocks, throwaway prototypes),
copy assets out and create static HTML files for the user to view. If working on
production code, copy assets and read the rules here to become an expert in designing
with this brand.

Key things to honour:
- The **white painted brush frame** over a sky-blue field is the signature look —
  use `BrushFrame` / `PortfolioPage`, vary the `seed` per page, never fake it with a
  plain border.
- Sky blue `#6AB9D9` fields, electric blue `#00ADEF` bands, orange `#F5821F` for data
  accents only. White rounded cards, soft shadows, no gradients.
- Archivo (uppercase, tracked-out display) + Mulish (body). Section titles ALL CAPS.
- Copy is first-person student voice, evidence-led, no emoji.

If the user invokes this skill without other guidance, ask what they want to build,
ask a few clarifying questions, then act as an expert designer who outputs HTML
artifacts or production code depending on the need.
